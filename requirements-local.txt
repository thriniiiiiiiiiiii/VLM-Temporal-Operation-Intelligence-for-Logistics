# ── Local dev requirements for pipeline testing (no GPU needed) ──────────────
# These are the packages needed to run: data_pipeline.py (dry-run),
# evaluate.py (import check), and scripts/ — without torch/transformers.

# Core data pipeline
opencv-python==4.13.0.92
numpy>=1.24
Pillow>=10.0
PyYAML>=6.0
tqdm>=4.64
scipy>=1.11
webdataset>=0.2.86
loguru>=0.7
rich>=13.0

# API testing (FastAPI routes check)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.7.0
httpx>=0.24.0

# Config / utilities
psutil>=5.9
